# Coffee Atlas 环境变量示例
# 复制为 .env / .env.development 并按需填写；.env* 已被 gitignore（.env.development 除外，它不含敏感值）

# ---- 数据源 ----
# true = 纯前端 Mock（无需启动 wrangler）；false = 走真实 API
# VITE_USE_MOCK=false

# ---- API（Phase 3 起）----
# 本地开发指向 wrangler dev（npm run dev:api）
# VITE_API_BASE_URL=http://localhost:8787
# 生产部署（前端与 API 同域）留空即可
